package org.prowl.distribbs.core;


/**
 * Sync is responsible for taking stock of a nodes messages and totalling each 'date block' up
 * So for example, Tuesday I had 114 news messages, Wednesday I had 34.  If the other node
 * has a different amount, then we send a list of messages, so we can find out what both sides are missing
 */
public class SyncTools {

}
